# Rover Control Module

This module controls a basic rover using GPIO-based motor drivers.

## Files
- `motor_driver.py`: Low-level motor control
- `rover_controller.py`: High-level movement logic
- `config.json`: Motor pin configuration

## Usage
Run `rover_controller.py` to execute a predefined movement sequence.
